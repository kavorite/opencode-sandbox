/* opencode-sandbox custom mbedTLS config — trimmed for TLS MITM proxy */
#ifndef MBEDTLS_CONFIG_H
#define MBEDTLS_CONFIG_H

/* System */
#define MBEDTLS_HAVE_ASM
#define MBEDTLS_HAVE_TIME
#define MBEDTLS_HAVE_TIME_DATE

/* TLS protocols */
#define MBEDTLS_SSL_PROTO_TLS1_2
#define MBEDTLS_SSL_PROTO_TLS1_3
#define MBEDTLS_SSL_CLI_C
#define MBEDTLS_SSL_SRV_C
#define MBEDTLS_SSL_TLS_C
#define MBEDTLS_SSL_SERVER_NAME_INDICATION

/* Key exchange */
#define MBEDTLS_KEY_EXCHANGE_ECDHE_RSA_ENABLED
#define MBEDTLS_KEY_EXCHANGE_ECDHE_ECDSA_ENABLED
#define MBEDTLS_KEY_EXCHANGE_RSA_ENABLED

/* PSA (required for TLS 1.3) */
#define MBEDTLS_PSA_CRYPTO_C
#define MBEDTLS_USE_PSA_CRYPTO
#define MBEDTLS_PSA_CRYPTO_CONFIG
#define MBEDTLS_PSA_CRYPTO_CLIENT

/* PSA algorithm support for TLS 1.3 */
#define PSA_WANT_ALG_SHA_256
#define PSA_WANT_ALG_SHA_384
#define PSA_WANT_ALG_HKDF
#define PSA_WANT_ALG_HKDF_EXTRACT
#define PSA_WANT_ALG_HKDF_EXPAND
#define PSA_WANT_ALG_HMAC
#define PSA_WANT_ALG_AES_GCM
#define PSA_WANT_ALG_ECDH
#define PSA_WANT_KEY_TYPE_AES
#define PSA_WANT_KEY_TYPE_ECC_KEY_PAIR
#define PSA_WANT_KEY_TYPE_ECC_PUBLIC_KEY
#define PSA_WANT_ECC_SECP256R1
#define PSA_WANT_ECC_SECP384R1

/* Crypto primitives */
#define MBEDTLS_AES_C
#define MBEDTLS_GCM_C
#define MBEDTLS_SHA256_C
#define MBEDTLS_SHA512_C
#define MBEDTLS_SHA1_C        /* needed for SKI/AKI X.509 extensions */
#define MBEDTLS_RSA_C
#define MBEDTLS_ECDSA_C
#define MBEDTLS_ECDH_C
#define MBEDTLS_ECP_C
#define MBEDTLS_ECP_DP_SECP256R1_ENABLED
#define MBEDTLS_ECP_DP_SECP384R1_ENABLED
#define MBEDTLS_BIGNUM_C
#define MBEDTLS_CTR_DRBG_C
#define MBEDTLS_ENTROPY_C
#define MBEDTLS_MD_C
#define MBEDTLS_OID_C
#define MBEDTLS_ASN1_PARSE_C
#define MBEDTLS_ASN1_WRITE_C
#define MBEDTLS_CIPHER_C
#define MBEDTLS_PEM_PARSE_C
#define MBEDTLS_PEM_WRITE_C
#define MBEDTLS_BASE64_C

/* PKI */
#define MBEDTLS_PK_C
#define MBEDTLS_PK_PARSE_C
#define MBEDTLS_PK_WRITE_C
#define MBEDTLS_PK_CAN_ECDSA_SIGN

/* X.509 */
#define MBEDTLS_X509_USE_C
#define MBEDTLS_X509_CRT_PARSE_C
#define MBEDTLS_X509_CRT_WRITE_C
#define MBEDTLS_X509_CREATE_C

/* Net */
#define MBEDTLS_NET_C

/* Misc */
#define MBEDTLS_ERROR_C
#define MBEDTLS_PLATFORM_C
#define MBEDTLS_VERSION_C
/* MBEDTLS_ENTROPY_HARDWARE_ALT removed — use platform entropy (/dev/urandom) */

/* MD algorithm capabilities */
#define MBEDTLS_MD_CAN_SHA256
#define MBEDTLS_MD_CAN_SHA512
#define MBEDTLS_MD_CAN_SHA1

/* PKCS1 for RSA */
#define MBEDTLS_PKCS1_V15
#define MBEDTLS_PKCS1_V21

/* GCM prerequisites */
#define MBEDTLS_CCM_GCM_CAN_AES

/* ECDH capability */
#define MBEDTLS_CAN_ECDH

/* TLS 1.3 prerequisites */
#define MBEDTLS_SSL_RECORD_SIZE_LIMIT
#define MBEDTLS_SSL_SESSION_TICKETS
#define MBEDTLS_SSL_KEEP_PEER_CERTIFICATE
#define MBEDTLS_SSL_EARLY_DATA
#define MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK_ENABLED
#define MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK_EPHEMERAL_ENABLED
#define MBEDTLS_SSL_ALPN_C

/* Reduce buffer sizes from 16KB default */
#define MBEDTLS_SSL_IN_CONTENT_LEN  8192
#define MBEDTLS_SSL_OUT_CONTENT_LEN 8192

/* NOT enabled (excluded to reduce binary size):
   MBEDTLS_CAMELLIA_C MBEDTLS_ARIA_C MBEDTLS_DES_C
   MBEDTLS_CHACHA20_C MBEDTLS_POLY1305_C MBEDTLS_DHM_C
   MBEDTLS_ECJPAKE_C MBEDTLS_LMS_C MBEDTLS_PKCS12_C
   MBEDTLS_PKCS5_C MBEDTLS_DEBUG_C */

#include "mbedtls/check_config.h"

#endif /* MBEDTLS_CONFIG_H */
